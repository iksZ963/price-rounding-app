import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appaaz.pennyroundingcalculator',
  appName: 'Penny Rounding Calculator',
  webDir: 'out'
};

export default config;
